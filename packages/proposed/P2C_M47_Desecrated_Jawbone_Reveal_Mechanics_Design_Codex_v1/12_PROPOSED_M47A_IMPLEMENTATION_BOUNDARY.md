# Proposed M47-A Implementation Boundary

## Conditional broad batch

After ChatGPT/User explicitly decides `M47-D1` through `M47-D6`, one coherent M47-A may implement together:

- `gnawed_jawbone`;
- `preserved_jawbone`;
- `ancient_jawbone`;
- base `reveal_desecrated`;
- canonical placeholder/install state transitions;
- row-driven item-level/MML rules;
- exact/oracle, seeded replay, diagnostics, negative controls, and M43-A one-step parity;
- fail-closed admission and atomic rollback.

This is broad enough to deliver a usable Desecration loop without mixing modifier-layer or Fracture disputes.

## Runtime authorization status

Not authorized. The exact side/replacement and Reveal-offer policies are load-bearing and must be selected before code is written. Building only a state shell now would add infrastructure without a truthful executable operation.

## Required acceptance evidence for a later implementation

- all three rows match their pinned data;
- partial/full side-capacity fixtures;
- fractured replacement protection;
- placeholder canonical/capacity invariants;
- ordinary and exclusive Reveal pool construction;
- family/group/MML behavior;
- complete-offer atomicity;
- direct/resolver/M43-A parity;
- exact mass conservation and seeded replay;
- full accepted-runtime regression;
- semantic fingerprint delta limited to the authorized rows.
