# Claude Audit Request

Please audit the proposed M46-A implementation and task framing, not merely the diff.

Verify at minimum:

1. only the clean base quarterstaff `fracturing_orb` is proposed;
2. all pinned input and disputed-state preconditions fail closed;
3. the candidate pool is combined across sides and uniform over installed instance identities, including crafted instances;
4. exactly one `fractured` flag changes and every other flag/state value is preserved;
5. exact rational mass, canonical terminal behavior, seeded replay, diagnostics, and negative control are load-bearing;
6. Annulment, Chaos, Perfect Essence, Alchemy, and capacity immutability regressions are sufficient;
7. resolver and M43-A parity use accepted shared seams and the executor registry fails closed;
8. the semantic fingerprint delta is limited to the authorized Fracture activation;
9. M46-A is still proposed, while Desecrated/Revealed/Jawbone/Reveal/PD-013 and every other boundary remain closed;
10. no public probability value or server-truth claim appears.

Return `GO`, `GO WITH CHANGES`, or `NO-GO`, include a plain-language summary for Kirill, and return all acceptance decisions to ChatGPT/User.
