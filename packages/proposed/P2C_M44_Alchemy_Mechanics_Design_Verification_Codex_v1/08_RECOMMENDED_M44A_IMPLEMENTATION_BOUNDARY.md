# Recommended M44-A implementation boundary

## One broad, coherent batch

Admit base `alchemy` for non-fractured quarterstaff items only:

- Normal or Magic input;
- discard all existing non-fractured explicit modifiers on the isolated copy;
- Rare output with exactly four newly generated modifiers;
- four sequential accepted ordinary weighted selections with branch-state pool rebuild;
- one atomic commit and one consumption boundary;
- exact/oracle, seeded MC, deterministic replay, diagnostics, mass-conservation, and negative-control evidence;
- resolver admission and executor-registry integration;
- one-step direct/resolver parity and M43-A one-step sequence parity after admission.

## Explicit gate statement required before implementation

ChatGPT/User should explicitly accept sequential shared ordinary-add sampling as the project-model implementation of the four-modifier roll. This is auditable and consistent with accepted kernels, but it is not claimed as server-truth exact probability behavior.

## Separately gated

- fractured Magic input behavior;
- Alchemy variants, Omens, Whittling, or other modifier layers;
- maps, jewels, and other special item classes;
- conditional/retry routes and route generation;
- optimizer/economics/advice/ranking;
- public numeric release;
- SOURCE/PROVENANCE, broader MML, crafted-capacity, or PD-013 closure.
