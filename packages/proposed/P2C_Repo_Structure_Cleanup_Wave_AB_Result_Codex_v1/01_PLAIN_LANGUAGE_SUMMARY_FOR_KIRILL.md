# Plain-Language Summary for Kirill

The recurring error was structural, not merely a model reading mistake. Git tracked four files in the folder named `work/active`, so an agent could mistake an old task record for the live instruction. Some first-read documents also repeated time-sensitive operation claims that later became stale.

This cleanup leaves one tracked live task file, teaches the validator to enforce that fact, and makes agents read the verified live dispatcher before general orientation prose. Stable documents now point to the accepted ledger and current canonical status instead of copying a runtime list that will age.

Nothing about crafting behavior changed. Historical packages, Claude reviews, and their references remain where they were. This result remains proposed until Claude audits it and ChatGPT/User accepts it.
