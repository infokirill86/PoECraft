# Unresolved Mechanics

These remain unresolved or explicitly project-policy rather than server truth.

## Uniformity of "random modifier"

Public wording says "random modifier"; it does not formally prove the internal server distribution.

Recommended project-model interpretation:

- base Chaos removal: uniform over eligible installed removable non-fractured modifier instances;
- base Annulment removal: uniform over eligible installed removable non-fractured modifier instances.

Remaining risk:

- the server could theoretically use side-first selection, hidden weights, or another internal process.

Required handling:

- keep this labeled project-model only;
- do not claim server truth;
- if exact server-truth probability is required later, require in-game/emulator sampling evidence.

## Whittling tie behavior

Public sources support lowest-level selection, but do not publish all tie behavior.

Current project policy:

- uniform among tied lowest-level eligible removable instances.

Required handling:

- keep as project policy / adopted inference;
- do not apply to base Chaos;
- require separate gate before Whittling runtime support.

## Greater / Perfect Chaos MML

Greater/Perfect Chaos rows have MML values in repo data and public currency text.

Current project state:

- MML is accepted as a project rule for greater/perfect chaos addition;
- MML is not server-truth closed;
- Greater/Perfect Chaos are still `admission_candidate`, not executable runtime.

Required handling:

- keep out of base M37-A unless explicitly authorized.

## Side Omens

PoE2 sources show side Omens for Chaos removal and Exalted add behavior.

Current project state:

- these are not base operation behavior;
- they require separate runtime admission if used.

Required handling:

- do not include in M37-A base Chaos unless a later gate explicitly expands scope.

