# Plain-Language Summary for Kirill

The easy part is now recorded: your existing answers about Reveal, the single Desecrated limit, Fracture interaction, and Echoes are accepted project rules, but still not executable M47 runtime.

D1-D5 are harder because they define the actual branching and therefore every later exact or Monte Carlo result.

The source check gives us useful partial answers:

- removal is described only when the whole item is full, so a lone free side should be used without deleting a modifier;
- a full item does lose a random modifier, but sources do not publish whether selection is one combined uniform pool or a side-first process;
- current community documentation and a recent large player dataset support an exclusive Desecrated offer, but official wording does not specify the exact composition algorithm;
- no trusted public source exposes the full Reveal weighting, without-replacement, blocking, and position algorithm;
- no source explains what the server does if a complete compatible offer set cannot be formed.

The practical conclusion is that D4 cannot be honestly proven by a few manual attempts. We either perform a large structured data collection, or you explicitly approve a source-aligned project model while keeping server truth open. The package makes that choice visible instead of silently copying YAML.

Claude audits next. M47-A remains closed.
