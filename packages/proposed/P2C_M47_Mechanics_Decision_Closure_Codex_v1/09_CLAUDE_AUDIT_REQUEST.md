# Claude Audit Request

Please audit the mechanics-closure evidence, experiment design, and decision boundary.

Verify:

1. the ratified rules are recorded exactly and are not mistaken for runtime admission or server truth;
2. D1-D5 evidence is exhausted proportionately across repo, official wording, PoE2DB/wiki, and supporting player data;
3. the package correctly resolves only the evidence-supported portion of D1 and the fact-of-removal portion of D2 while leaving probability models open;
4. the candidate YAML is compared but never promoted automatically;
5. D3's evidence strength and quarterstaff-specific limitation are described honestly;
6. D4 is correctly identified as not closable by a few manual trials;
7. every in-game protocol has a reproducible setup, action, observable, repetition count, and competing-model interpretation;
8. the smallest decision table is sufficient before M47-A;
9. the conditional M47-A contract remains one coherent base Jawbone/Reveal batch with every modifier and Fracture extension excluded;
10. no numeric probability, runtime implementation, operation admission, or boundary closure leaked into the package.

Return `GO`, `GO WITH CHANGES`, or `NO-GO`, include a plain-language summary for Kirill, and return D1-D5 selection and any M47-A authorization to ChatGPT/User.
