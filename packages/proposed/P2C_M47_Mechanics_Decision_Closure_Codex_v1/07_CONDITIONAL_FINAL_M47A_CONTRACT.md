# Conditional Final M47-A Contract

If and only if D1-D5 are explicitly selected after Claude audit, the broad M47-A runtime floor may contain:

- `gnawed_jawbone`, `preserved_jawbone`, and `ancient_jawbone` for Rare quarterstaff;
- one data-driven Jawbone executor;
- one canonical `DesecratedPlaceholder` with fixed side, source row, MML, and no modifier-layer constraint in base M47-A;
- row item-level and Ancient MML rules;
- the selected D1 side policy and D2 full-item replacement policy;
- fractured replacement exclusion;
- one base Reveal executor through the canonical ordinary-plus-exclusive pool;
- the selected D3 composition, D4 sampling, and D5 failure policies;
- three offers followed by one user-selected canonical installed modifier with `desecrated: true`;
- one-Desecrated/no-rune invariant;
- atomic no-transition/no-consumption failures;
- exact/oracle and seeded replay through one shared kernel;
- direct/resolver/M43-A one-step parity and fail-closed registry admission.

## Explicit M47-A exclusions

- Echoes runtime;
- Omen of Light;
- Necromancy and named-Lich Omens;
- Putrefaction and multiple placeholders;
- combined Fracture runtime extension, despite the ratified future rule;
- conditional routes, planner/optimizer/economics/advice;
- public numeric probability release;
- boundary closure.

M47-A remains unauthorized by this package.
