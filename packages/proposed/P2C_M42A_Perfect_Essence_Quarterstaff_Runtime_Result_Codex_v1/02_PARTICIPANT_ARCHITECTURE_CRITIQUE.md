# Participant and architecture critique

No material objection remains after the gate explicitly selected both probability-affecting rules.

The six-row batch is correctly sized because all rows share the same mechanics and differ only in canonical guaranteed-output data. One shared executor is safer than copied row functions.

The implementation deliberately filters the accepted shared removal pool by complete terminal validity. It does not build a parallel modifier/capacity model. Omens remain a separate resolver layer, and `crafted_count == 0` prevents this floor from silently inventing replacement or repeat behavior.
