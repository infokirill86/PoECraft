# Claude audit request

Please audit M42-A implementation and task framing, then return `GO`, `GO WITH CHANGES`, or `NO-GO`.

Verify:

1. exactly six prepared rows are admitted and no Perfect Seeking/Infinite is invented;
2. all six use one shared executor and the accepted resolver;
3. feasible removal starts from the shared removal pool and validates complete branch terminals through canonical state validation;
4. free-side and full-side capacity behavior matches the ratified project rule;
5. selection is uniform over feasible non-fractured instance identities and exact/terminal mass normalizes;
6. `crafted_count == 0` is enforced before draw and no replacement/repeat behavior exists;
7. the exact guaranteed modifier is installed without an add-pool draw;
8. atomicity, no-consumption failure, fractured preservation, replay, and negative controls are load-bearing;
9. semantic fingerprint and scope changes contain only the authorized surface;
10. M42-A remains proposed and all listed boundaries remain closed.

Include a plain-language summary for Kirill. Acceptance authority remains ChatGPT/User.
